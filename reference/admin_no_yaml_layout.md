# Missing Config Layout

This is a UI / Layout function for the Admin Console

## Usage

``` r
admin_no_yaml_layout()
```

## Value

an HTML tag (div)

## Examples

``` r
if (FALSE) { # \dontrun{
admin_no_yaml_layout()
} # }
```
