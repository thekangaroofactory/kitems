# YAML Messages

This is a helper function to return messages about the YAML config.

## Usage

``` r
admin_yaml_message(config)
```

## Arguments

- config:

  a YAML config

## Value

an HTML tag

## Examples

``` r
if (FALSE) { # \dontrun{
admin_yaml_message(config)
} # }
```
