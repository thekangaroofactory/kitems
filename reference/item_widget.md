# Item Widget

The UI component for the item table.

## Usage

``` r
item_widget(id)
```

## Arguments

- id:

  the id of the module server instance.

## Value

An HTML tag.

## Examples

``` r
if (FALSE) { # \dontrun{
item_widget(id = "mydata")
} # }
```
