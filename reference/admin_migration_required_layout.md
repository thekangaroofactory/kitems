# Migration Required Layout

Migration Required Layout

## Usage

``` r
admin_migration_required_layout(files)
```

## Arguments

- files:

  a list of data.model files to migrate

## Value

a layout

## Examples

``` r
if (FALSE) { # \dontrun{
admin_migration_required_layout("./item_data_model.rds")
} # }
```
